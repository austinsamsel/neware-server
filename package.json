{
  "name": "neware-server",
  "version": "1.0.0",
  "description": "server layer for neware",
  "main": "index.js",
  "scripts": {
    "start": "node --harmony-async-await index",
    "deploy": "now -e GREETING=@greeting",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hightops",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^4.0.0",
    "firebase": "^3.6.8",
    "firebase-admin": "^4.1.0",
    "micro": "^6.2.1"
  }
}
